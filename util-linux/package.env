# util-linux
PKG_VER=2.40.2
PKG_URL="https://github.com/util-linux/util-linux/archive/refs/tags/v${PKG_VER}.tar.gz"
PKG_DEPS="lfs/bison:native lfs/gettext:cross"
BUILD_PROCESS=configmake
#PKG_AUTOCONF=1
CONF_FLAGS="ADJTIME_PATH=/var/lib/hwclock/adjtime \
            --with-systemdsystemunitdir=/lib/systemd/system \
            --disable-chfn-chsh \
            --disable-login \
            --disable-nologin \
            --disable-su \
            --disable-setpriv \
            --disable-runuser \
            --disable-pylibmount \
            --disable-makeinstall-chown \
            --without-tinfo"
CONF_VARS="ADJTIME_PATH=/var/lib/hwclock/adjtime runstatedir=/run"
PKG_MAKEVARS="NCURSES_LIBS=-lncurses"
### The cross build (libuuid and libblkid only) and the full build are in variants/target/,
### the warning flags of each toolchain in variants/toolchain/

#PKG_TOOLCHAIN=gnu
