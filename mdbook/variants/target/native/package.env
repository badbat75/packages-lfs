# mdbook, target native: installed once into the global toolchain
### "mdbook --version" prints "mdbook v<version>"
PKG_CHECK="[ x\$( ${INSTALL_EXECPREFIX}/bin/mdbook --version | head -n1 | awk '{print \$NF}' ) == xv${PKG_VER} ]"
