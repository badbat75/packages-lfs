# libxml2, target bootstrap
### Not the default build: the values of package.env this target keeps or drops
PKG_DEPS="lfs/ncurses lfs/readline"
CONF_FLAGS="--with-history --with-threads"

CONF_FLAGS+=" --without-python"
