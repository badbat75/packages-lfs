# cpio, target native: the cpio that packs the initramfs kernelbuild makes on the host (dracut)
PKG_CHECK="[ \"\$( ${INSTALL_EXECPREFIX}/bin/cpio --version 2>/dev/null | head -n1 )\" == \"cpio (GNU cpio) ${PKG_VER}\" ]"
