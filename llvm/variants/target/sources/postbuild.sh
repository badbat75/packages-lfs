# shellcheck shell=bash
# shellcheck disable=SC2154
# llvm, target sources: nothing is installed, so the postbuild of target/sysroot (sed on the
# installed cmake files) does not apply: this empty script replaces it
