# llvm, target sources: download and extract only, no build, no install: the sources of the
### llvm-project, for packages that use them in place (lfs/libclc builds its libclc with the
### native clang out of this tree)
### Not the default build: the values of package.env this target keeps or drops
PKG_DEPS=
BUILD_PROCESS=none
