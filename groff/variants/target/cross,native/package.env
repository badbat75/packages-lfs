# groff, target cross or native
### Not the default build: the values of package.env this target keeps or drops
PKG_DEPS=
PKG_MAKEVARS=

PKG_CHECK="[ x\$( ${INSTALL_EXECPREFIX}/bin/groff -V | awk 'NR==1{print \$4}' || true ) == x${PKG_VER} ]"
