# Tcl, target native: the tclsh of the build host (the TCL bindings of SQLite run it)
PKG_CHECK="[ x$(echo 'puts [info patchlevel]' | ${GLOBAL_TOOLCHAIN_PATH}/bin/tclsh 2>/dev/null || :) == x${PKG_VER} ]"
