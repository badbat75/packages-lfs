# libisl, target native
### Not the default build: the values of package.env this target keeps or drops
PKG_DEPS=

PKG_CHECK="[ \"$(PKG_CONFIG_LIBDIR=${INSTALL_LIBDIR}/pkgconfig pkg-config --modver isl 2>/dev/null || :)\" == \"${PKG_VER}\" ]"
