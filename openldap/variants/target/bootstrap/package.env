# openldap, target bootstrap: libraries only, without slapd and cyrus-sasl (which depends on it)
PKG_DEPS="lfs/openssl"
CONF_FLAGS="${OPENLDAP_COMMON_FLAGS} --disable-slapd"
PKG_MAKETARGETS="install"
