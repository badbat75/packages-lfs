# Mesa, target bootstrap
### no LLVM in the sysroot yet: llvmpipe (a separate driver since 24.3) cannot be built here
CONF_FLAGS+=" -Dgallium-drivers=svga,softpipe,virgl"
CONF_FLAGS+=" -Dllvm=disabled"
CONF_FLAGS+=" -Dvulkan-drivers=\"\""
CONF_FLAGS+=" -Dlibunwind=disabled"
