# gobject-introspection, target cross
### Not the default build: the values of package.env this target keeps or drops
PKG_DEPS="lfs/bison:native lfs/flex:native"
PKG_MAKEENV=

PKG_DEPS+=" lfs/GLib:cross"
CONF_FLAGS="-Dpython=python3"
