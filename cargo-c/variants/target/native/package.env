# cargo-c, target native: installed once into the global toolchain
### The version carries the cargo it was built against: "cargo-c 0.10.25+cargo-0.99.0"
PKG_CHECK="[ x\$( ${INSTALL_EXECPREFIX}/bin/cargo-cbuild --version | head -n1 | awk '{print \$NF}' | cut -d+ -f1 ) == x${PKG_VER} ]"
