# libmpfr, target native
### Not the default build: the values of package.env this target keeps or drops
PKG_DEPS="lfs/libgmp"

PKG_CHECK="[ \"$(PKG_CONFIG_LIBDIR=${INSTALL_LIBDIR}/pkgconfig pkg-config --modver mpfr 2>/dev/null || :)\" == \"${PKG_VER}\" ]"
