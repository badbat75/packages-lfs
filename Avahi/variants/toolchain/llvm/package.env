# Avahi, toolchain llvm: single quotes, llvm-config runs when runmake.sh does (with the build PATH)
PKG_MAKEENV='LD_LIBRARY_PATH=$(llvm-config --libdir):${SYSROOT}${INSTALL_LIBDIR}${INSTALL_LIBSUFFIX}'
